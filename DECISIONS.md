# MaddPrestige V2 Decision Log

**Owner approval recorded:** 2026-08-15
**Status legend:** `Mandated` comes directly from the master specification; `Owner-approved` was explicitly approved by the owner; `Deferred` is intentionally unresolved until its named phase/evidence exists.

| ID | Status | Decision | Rationale / consequence |
|---|---|---|---|
| D-001 | Mandated | `MaddPrestige_V2_Master_Spec.md` is authoritative; kickoff governs Phase 0 execution. | Resolves conflicts with V1 code/docs. |
| D-002 | Mandated | Generic Java core; MaddKraft branding/ranks/balances/features live in an optional preset. | Enables public use and removes obsolete assumptions. |
| D-003 | Mandated | LuckPerms groups are consumed, never created. MaddPrestige manages only direct memberships in explicitly configured progression groups. | Protects hierarchy, weights, prefixes, supporter/staff/specialist/event groups. |
| D-004 | Owner-approved | Default LuckPerms reconciliation policy is `warn-only`. | Safest fresh migration posture; avoids feedback loops and silent external repair. |
| D-005 | Owner-approved | Baseline/default stages may use `projection: none`; MaddKraft Wanderer/default uses `projection: none`. | Avoids treating LuckPerms `default` as owned membership. |
| D-006 | Mandated | Old progression groups (`odd`, `mad`, `unbound`) and old patron groups are obsolete and never auto-created. | Legacy data requires explicit mapping/report. |
| D-007 | Owner-approved | Modernize incrementally with a frozen V1 fixture, not a big-bang rewrite or piecemeal production patch. | Preserves useful behavior while establishing safe boundaries. |
| D-008 | Owner-approved | Retain Maven and create modules `maddprestige-api`, `maddprestige-core`, `maddprestige-persistence`, `maddprestige-platform-paper`, `maddprestige-integrations`, `maddprestige-distribution`, and `maddprestige-testkit`. | Avoids needless Gradle migration and enables testable boundaries. |
| D-009 | Owner-approved | Initial platform baseline is Java 25 and Paper 26.1.2. Do not claim broader compatibility until explicitly tested and qualified. | Matches the verified environment (current retained build 74) without overstating support. |
| D-010 | Owner-approved | One schema registry drives YAML, commands, GUI, validation, help, diff, and docs. | Required for functional parity and prevents drift. |
| D-011 | Owner-approved | YAML is edited through a comment/order/unknown-key-preserving document model and named draft/revision workflow; Bukkit `FileConfiguration` is not the canonical writer. The library choice is deferred until the Phase 1 spike and golden tests pass. | Prevents current comment/key corruption and makes the round-trip spike a hard gate. |
| D-012 | Owner-approved | Runtime consumes one immutable compiled `ActiveConfiguration` pinned by revision/content hashes. | Makes apply/rollback and operation recovery coherent. |
| D-013 | Owner-approved | Provider registry uses typed capabilities, health, provenance, and lazy activation; arbitrary YAML event reflection is removed. | Enables extension without silent class/method drift or spoofed progress. |
| D-014 | Mandated | QuickShop and other P2P volume provide zero progression-income credit by default. | Prevents circular/collusive turnover exploits. |
| D-015 | Owner-approved | Internal state uses immutable lowercase IDs limited to the conservative `a-z0-9._-` syntax, exact decimal currency storage, optimistic state revisions, and normalized history/baseline/operation records. | Decouples display/external IDs and protects integrity. |
| D-016 | Owner-approved | Consequential changes use immutable operation plans with persisted per-action state, idempotency keys, verification, and reconciliation. | Provides crash recovery without falsely promising exactly-once external commands. |
| D-017 | Owner-approved | SQLite is the default single-server backend and uses a serialized bounded write coordinator. MySQL and MariaDB may implement the same contracts, but each requires contract testing if publicly claimed. | Backend support does not imply multi-server/network-safe progression. |
| D-018 | Owner-approved | Database migrations are ordered/checksummed, backup-gated, dry-runnable, and reported; schema versions are never overwritten opportunistically. | Fixes current version-4 false-current risk. |
| D-019 | Owner-approved | Staff observation, draft edit, apply, rollback, player mutation, force, migration, recovery, and debug have separate permissions. | Removes GUI privilege escalation and supports least privilege. |
| D-020 | Owner-approved | PAPI output is cache-only; PAPI input is a separately configured sampled/typed provider that fails closed. | Protects render performance and makes outages actionable. |
| D-021 | Owner-approved | Generic command actions are disabled in the safe default, require explicitly allowlisted roots/templates, and expose uncertainty honestly. | A denylist cannot make arbitrary console commands safe; universal exactly-once behavior is not claimed. |
| D-022 | Mandated | Rabbit Holes, decrees, bosses, resource-world lifecycle, Court drafting, and PvP scoring are not core mandatory progression systems. | They may be future optional providers only. |
| D-023 | Owner-approved | Phase 1 builds foundations only and never touches production data or existing schema except disposable fixtures. | Preserves the Phase 0/1 safety gate. |
| D-024 | Owner-approved | Maven group is `net.maddkraft`; Java root package is `net.maddkraft.maddprestige`. | The developer/product namespace may carry MaddKraft identity, but generic core gameplay remains unbranded and configuration-driven. |
| D-025 | Owner-approved | Use the canonical file split proposed in Phase 0. | Avoids a monolithic configuration while preserving one canonical model. |
| D-026 | Owner-approved | Network/proxy synchronization is outside V2 core unless separately designed and qualified. | MySQL/MariaDB availability must not be advertised as network-safe progression. |
| D-027 | Owner-approved | Competitions are disabled in the generic default; a generic optional module may exist, while MaddKraft competition identity is deferred. | Removes hardcoded MaddHatter behavior from core. |
| D-028 | Owner-approved | Retention is configurable; revisions/backups/migration artifacts referenced by active or pending work are protected from pruning; secrets are redacted from every output surface. | Phase 1 establishes policy/schema, with user-facing tuning later. |
| D-029 | Owner-approved | Persistence must support documented player-data inventory, export, safe deletion/anonymization, and history/audit retention without breaking referential integrity. | Public-facing tooling is primarily Phase 8, but the storage model must enable it. |
| D-030 | Owner-approved | Do not infer legacy rank mappings. If no production data exists, freeze available V1 artifacts and use synthetic edge-case migration fixtures; real data elsewhere remains untouched and must be backed up before later migration work. | Lack of a production fixture does not block Phase 1 foundations. |
| D-031 | Phase 1 implementation | Select SnakeYAML Engine 3.0.1 for YAML 1.2 parsing/marks and a MaddPrestige-owned lossless source document for narrow scalar edits. Every edit reparses; block scalars, anchored scalars, and explicitly tagged scalars fail closed. | Hardened golden/regression tests prove comments, order, dotted/nested/unknown keys, CRLF, supplementary Unicode, safe quoting, scalar styles, and multiple documents without trusting a destructive whole-document emitter. |
| D-032 | Phase 1 implementation | The root becomes the seven-module Maven parent; the distribution module compiles unchanged V1 sources/resources/tests from their original root paths and bundles inactive V2 foundations. | Establishes approved modules without a big-bang V1 move or activating V2 against live data. |
| D-033 | Phase 1 implementation | SQLite is the only locally implemented/tested V2 backend. MySQL 8.4.10 and MariaDB 11.8.8 CI definitions are contract harness targets only and are not support claims. | Local Docker/services are absent; compilation or primitive CI probes cannot establish full repository/backend compatibility. |
| D-034 | Phase 1 implementation | The verified byte-copy backup service is restricted to quiesced/disposable files. Live SQLite WAL backup requires a later coordinated online-backup/checkpoint implementation and remains fail-closed. | Avoids presenting a byte copy of an active WAL database as a production-safe backup. |
| D-035 | Phase 1 correction | SQLite migration preflight is read-only; required verified backup precedes migration-history DDL. Only `APPLIED` versions are unique through a partial index, failed attempts remain repeatable, and applied history must be an exact prefix of the requested chain. | Preserves the backup-first invariant, auditability, safe retry, deterministic ordering, and protection against running an older missing migration after newer history. |
| D-036 | Phase 2 implementation | `progression.yml` is compiled through the Phase 1 canonical draft/revision service into an immutable ordered stage snapshot. Fresh defaults are inactive; active deferred Phase 3+ fields fail validation. | Adds arbitrary stages without a second configuration system or misleading placeholder gameplay behavior. |
| D-037 | Phase 2 implementation | One rank adapter may project an ordered ladder in Phase 2; individual stages may use `projection: none`. Configuration candidates pin the externally validated provider generation, and every operation pins the complete managed group set, desired projection, config revision, and provider generation. | Keeps baseline behavior configurable while making validation/apply/reload races and ownership scope explicit. Multi-adapter ladders remain unsupported rather than partially activated. |
| D-038 | Phase 2 implementation | LuckPerms ownership is limited to exact permanent, context-free direct inheritance nodes in the revision-pinned managed set. Contextual/temporary managed nodes are ambiguous and preserved; every configured target is loaded/validated before mutation. | Enforces the narrowest provable ownership boundary and prevents a bad target from stripping prior managed membership. |
| D-039 | Phase 2 implementation | UUID-first player stage state uses optimistic revisions. Authoritative repairs use persisted operation/action state; save or mid-generation uncertainty becomes `NEEDS_RECONCILIATION`. Import-once inserts only an absent internal record from one unambiguous mapped group. | Prevents display/order drift, feedback loops, stale writes, blind replay, and false success. |
| D-040 | Phase 2 owner-review correction | A revisioned stage remap plan records intent but never proves player rows were remapped. While removed/disabled stages have nonzero stored references and Phase 2 has no atomic remap executor, configuration activation remains blocked even when the plan is complete. | Preserves authoritative player-stage referential safety without pulling production bulk migration into Phase 2. |
| D-041 | Phase 3 implementation | Requirement values are provider-advertised typed metrics; trees reference immutable requirement IDs and baseline/latch identity uses the versioned `rsf2:` SHA-256 of a deterministic length-prefixed binary encoding of every behavioral field. | Prevents permissive string coercion, structurally ambiguous filter serialization, and a changed operator/target/scope/scaling/catch-up policy inheriting stale completion state. |
| D-042 | Phase 3 implementation | Requirement evaluation is pure. Lifecycle services initialize baselines and persist eligible latches explicitly at owned boundaries using UUID, measurement scope, scope-instance ID, semantic fingerprint, and provider generation; returned records must match the exact requested key. | Simulation/repeated reads cannot mutate progress, while stage/prestige/season instances remain isolated and resettable without deleting unrelated history. The services and persistence semantics are tested, but no production Phase 3 lifecycle owner invokes them yet; future stage-runtime composition owns stage entry/completion calls and Phase 4 owns Prestige/season calls. |
| D-043 | Phase 3 implementation | Scaling is applied before opt-in catch-up. Linear, exponential, and stepped formulas use exact decimal arithmetic with explicit index, parameter precision/scale, predicted exponential precision-work/magnitude, final magnitude, rounding, and quantum bounds; catch-up reduction is capped and floored. | Makes targets deterministic/auditable and rejects pathological `BigDecimal.pow` work before computation without clamping. |
| D-044 | Phase 3 implementation | Only the canonical rank-up authorization service may issue executable authority. Public `RankUpIntent` carries actor/player/optional target/idempotency intent only; engine-injected progress-context and requirement-state sources own scaling, catch-up, scope identity, baselines, and latches. Authorization resolves the legal next enabled stage and exact tree/costs/rewards from the atomic active snapshot. A plan separately binds the player's observed source-state config provenance and the current active config revision governing the operation; successful commit CAS-checks the former and writes the latter. Configured rank projection must be active, generation-pinned, healthy, and implement `RankAdapter` during planning/simulation. A blocked canonical plan carries denied authority. An executable seal binds identity/stages/revisions/provider generations, complete requirement result and provenance, costs/rewards, configured and requested projection, blockers/execution allowance, and the operation plan; diagnostic-only unavailable-provider reporting is not consumed by execution and is deliberately outside the seal. Execution validates the seal before persistence, then orders costs → external projection → optimistic internal commit → rewards. | Prevents caller-fabricated progress state, safe config applies freezing unchanged player stages, inaccurate simulation during rank-provider outage, blocked-plan escalation, requirement-outcome or projection substitution, fabricated/stale evaluations, skipped actions/stages, partial effects, false success, and unsafe replay. External-first ordering makes external-success/internal-failure explicit; the inverse cannot be created by this executor and existing reconciliation handles preexisting divergence. |
| D-045 | Phase 3 implementation | Authorized command rewards use reviewed templates and structured exact tokens. Policy is disabled by default, normalizes roots, applies mandatory dangerous-root precedence, enforces operation-wide count/length bounds, and checks maximum recursion against a trusted runtime `CommandExecutionContext` whose correlation is retained and whose nested depth can only increase. Commands remain non-idempotent/non-reversible with external uncertainty. | Prevents configuration metadata from claiming a false trigger depth and avoids turning configuration into arbitrary console, shell, SQL, filesystem, reflection, or code execution. Phase 3 proves context propagation at the provider boundary; production command-triggered rank-up ingress remains Phase 6. |
| D-046 | Phase 3 implementation | Vanilla statistics are discovered from the real Bukkit API, advertise separately classified block/item/entity dimensions, and read in one Paper-thread batch. Manual progress bootstrap issues a non-serializable owner capability; provider/metric/generation-scoped handles and capability-attested provenance authorize bounded in-memory aggregation, versioned batch persistence, and flush/shutdown barriers. Human-readable owner identity is metadata only. | Avoids invented/invalid capabilities, main-thread/provider-call fan-out, spoofed API progress, registration races, unbounded caches, and SQL-per-event behavior. |
| D-047 | Phase 3 implementation | `requirements.yml`, `rewards.yml`, and schema-3 stage references compile through the existing canonical draft/revision workflow. One atomic `ActiveStageConfiguration` publishes same-revision Stage and Phase 3 snapshots, and the Phase 3 snapshot retains the exact active/referenced provider-generation map validated during prepare/apply, including configured rank projection. Runtime never reconstructs those pins from the mutable registry. Dormant definitions remain schema/type validated without activating providers; absent optional reward providers do not block apply or receive a pin. | Prevents a parallel configuration truth, mixed revisions, silently rewritten pins, eager dormant-provider coupling, and inconsistent optional-reward semantics. |
| D-048 | Phase 4 implementation | Prestige is a distinct intent-only authorization, immutable simulation, opaque authorization, plan, executor, and persistence lifecycle built over the shared operation journal; it is not disguised as a rank-up. | Prestige resets stage, scope, currency, milestone, and count state together and therefore has materially different confirmation, transaction, and recovery semantics. |
| D-049 | Phase 4 implementation | Prestige execution orders journal insertion → verified costs → configured managed-rank projection → one atomic internal commit → rewards. The internal transaction CAS-updates stage and current/lifetime Prestige, creates the next Prestige scope/baselines, resets configured internal currency, records milestone awards, and appends stage/Prestige history. | Required rewards are preflighted before mutation. External projection failure cannot produce false internal success; external success followed by failed/unknown internal commit and post-commit reward uncertainty are explicit reconciliation states. |
| D-050 | Phase 4 implementation | Every resettable component has an explicit `RESET`/`PRESERVE` classification. Stage resets and historical-statistics preservation are mandatory invariants; a successful Prestige creates a new immutable scope identity instead of deleting prior baseline/latch/history rows. | Old scoped state remains auditable but cannot leak into `SINCE_PRESTIGE_START`. External provider reset authority is separate, unsupported, and disabled in Phase 4. |
| D-051 | Phase 4 implementation | Internal currency uses stable `CurrencyId`, exact decimal text, bounded scale/precision/balance policy, transactionally maintained account plus append-only ledger, and unique operation/action idempotency. Cost and reward roles use separate providers because their Java batch interfaces erase to incompatible `List` signatures. | Display/symbol edits cannot rename data; no floating point, negative balance, unaudited adjustment, or replay double debit/credit is permitted. SQLite transactions/idempotency are authoritative; synchronization only reduces local contention. |
| D-052 | Phase 4 implementation | Entitlements are typed values with deterministic source ordering and explicit duplicate-source rejection. Built-in merge strategies are `MAX`, overflow-safe `SUM`, `MIN`, priority-ordered `OVERRIDE`, and `BOOLEAN_OR`, all returning source provenance. | Prevents map-iteration nondeterminism and accidental additive stacking when configuration intends a maximum. Provider-defined strategy extension remains unfrozen until the later SDK phase. |
| D-053 | Phase 4 implementation | Milestone identity and repeatability keys (`ONCE`, `ONCE_PER_PRESTIGE`, `ONCE_PER_SEASON`) are persisted in the same internal Prestige transaction. Prestige-count milestone rewards are folded into canonical preflight and the operation plan. | One-time awards cannot be redelivered after restart. Stage/season/provider trigger shapes are modeled, but their separate lifecycle award hooks are not falsely executed by the Prestige service. |
| D-054 | Phase 4 implementation | Seasons are optional one-active-at-a-time progression-data containers. Start allocates a new scope, player entry initializes real season baselines, end archives before a replacement can start, and reset/preserve of season progress is explicit. | SQLite uniqueness plus lifecycle compare-and-set protects the one-active rule; no world, PvP, Court, or resource-world ownership is implied. |
| D-055 | Phase 4 implementation | Startup recovery scans a bounded indexed set of incomplete operations and makes operation-type-aware decisions from persisted operation/action/internal-commit evidence. It verifies known internal effects without replay, abandons unexecuted prepared work, and marks interrupted or incomplete external effects uncertain/reconcilable. Recovery decisions are appended to their own audit table. | Exactly-once is claimed only for native transaction/idempotency effects. Unknown external effects are never blindly replayed or described as rolled back. |
| D-056 | Phase 4 implementation | Competition has a real disabled/default-off boundary. Unsupported enabled configuration fails validation and construction; no competition service, command, UI, lifecycle, or named legacy competition exists. | This satisfies disabled-behavior A34 without claiming the larger optional competition framework from Section 18. |
| D-057 | Phase 4 implementation | `lifecycle.yml` compiles through the existing immutable compiled-document boundary, Phase 4 extends the canonical schema, semantic validation checks stage/reward/provider references, and Phase 4 snapshots retain every prior provider-generation pin while allowing additional Phase 4 pins. | Preserves lossless draft/revision ownership and prevents a parallel ad-hoc runtime configuration or mixed provider generations. V2 remains deliberately unconnected to the frozen production bootstrap. |
| D-058 | Phase 4 correction | Prestige requirement progress, baselines, and latches form one coherent scoped-state disposition: all `RESET` creates a new scope and reachable boundaries; all `PRESERVE` retains the existing scope and writes no boundaries. Stage is reset-only; purchased perks, milestone history, season progress, and historical statistics are preserve-only. | Simulation and execution cannot diverge through contradictory or unimplemented component policies. Historical evidence is retained rather than deleted. |
| D-059 | Phase 4 correction | Phase 4 provider activation is reachability- and role-local. The enabled Prestige tree is traversed transitively, exact generations are pinned only for consumed providers, and planning plus post-cost execution require active healthy bindings. | Dormant/optional providers remain lazy, while a required reset-rank or boundary metric provider cannot yield an executable stale/unhealthy confirmation. |
| D-060 | Phase 4 correction | Native currency provenance is carried by sealed planned actions. SQLite transactions, uniqueness, and bounded whole-transaction contention retry are the cross-instance concurrency boundary. Season entry plus required baselines and normal rank-up stage CAS plus history each commit atomically. | Removes mutable-revision provenance and process-local concurrency assumptions, and closes the season/stage-history crash boundaries. |
| D-061 | Phase 4 correction | Recovery stores provider/action characteristics and exact sealed native cost/reward payloads. Only healthy generation-matching native recoverable providers may replay idempotent rewards or compensate known applied reversible/idempotent costs; unknown external actions remain uncertain. | A59 is supported without weakening A60 or fabricating a new plan after restart. |
| D-062 | Phase 4 correction | A16 and A17 remain `Partial`: engine + persistence lifecycle semantics are implemented and restart-tested, but production runtime composition remains later. | Applies the same project acceptance criterion already used for A14/A15 and avoids pulling later bootstrap work into Phase 4. |
| D-063 | Phase 4 correction pass 2 | Recovery may terminally declare `COMPENSATED` only after every potentially consequential cost action is proven absent or safely reversed. Safe native compensation may proceed while another `STARTED`/`UNCERTAIN` cost keeps the operation in `NEEDS_RECONCILIATION`. | Prevents a mixed native/external pre-commit crash from falsely claiming the whole operation was reversed; retry retains the evidence and never duplicates compensation. |
| D-064 | Phase 4 correction pass 2 | Season configuration models only its owned `SEASON_PROGRESS` disposition. The season YAML policy accepts exactly `season-progress: RESET|PRESERVE`; unrelated reset components are compiler errors. Player entry qualifies `ACTIVE` inside its write transaction, and progress mutation conditionally requires `ACTIVE` in the authoritative update. | Removes inert configuration, linearizes entry against archive, and makes archived history immutable while retaining exact reset/preserve carry-forward semantics. |
| D-065 | Phase 4 correction pass 2 | Currency operation/action replay equivalence binds player, currency, exact delta, mutation kind, configuration revision, actor type/UUID/name, source, and reason. Retry-only `occurredAt` does not participate. | The same idempotency key cannot relabel an audited mutation, while a legitimate later retry still resolves deterministically to the one stored effect. |
| D-066 | Phase 4 correction pass 2 | Stage-history actor UUID is nullable but durable. Migration 5 adds `actor_uuid`; both normal rank-up and Prestige-reset writers bind it, and reads reconstruct the complete `Actor`. | Full actor identity survives close/reopen without invalidating console/system rows that legitimately have no UUID. |
| D-067 | Phase 5 implementation | Optional integration discovery uses the existing `ProviderRegistry`: adapters register inactive, only configuration-reachable IDs activate, disable unregisters exact bindings, and rebind creates a new generation. | A missing/dormant plugin cannot become globally critical, while an active plan cannot silently substitute a recovered dependency generation. |
| D-068 | Phase 5 implementation | Vault economy roles are separate `CostProvider`, `RewardProvider`, and optional balance `MetricProvider` implementations over one immutable Vault service binding. All calls use a controlled server-thread scheduler and reject non-positive, over-scale, non-finite, or double-inexact amounts. | Java erasure prevents combining cost/reward batch contracts. Vault provides no operation key or truthful compensation API, so adapter actions are explicitly non-idempotent, non-reversible, non-reconcilable, and externally uncertain; the MaddPrestige journal remains the normal exactly-once authority. |
| D-069 | Phase 5 implementation | mcMMO integration is read-only for absolute skill/power metrics. Adjusted XP uses the official final gain event to increment the existing capability-authenticated manual progress provider; scoped delta comes from accepted baseline semantics. | Current-level XP can roll over without losing Prestige-scope progress, while MaddPrestige never edits or resets mcMMO storage/progression. |
| D-070 | Phase 5 implementation | PlaceholderAPI output and input are asymmetric. Output is a persistent expansion backed only by bounded immutable canonical snapshots; input is explicitly configured, scheduled, typed, bounded and freshness-checked, and never resolves PAPI during a requirement read. | Placeholder rendering cannot become a DB/provider hot path, and player-controlled/missing/recursive/unparseable/stale data cannot become consequential fail-open authority. |
| D-071 | Phase 5 implementation (superseded by D-078) | EconomyShopGUI 7.2.0's compatible published API 1.10.1 post-transaction contract was initially modeled as an opt-in event-tracked sale metric. | Correction review proved its heterogeneous economy units cannot support that claim; D-078 withdraws the metric and retains diagnostics-only compatibility. |
| D-072 | Phase 5 implementation | QuickShop-Hikari 6.2.0.11 is compatibility-only. Its public success event may feed bounded diagnostics, but the integration exposes no metric/reward/manual-progress handle; configured progression credit is fixed false and any true value is rejected. | Self-trading, alts, circular movement, wash trading, resale, duplication, refunds or restart gaps cannot produce MaddPrestige progression because every transaction receives exactly zero credit. |
| D-073 | Phase 5 implementation | PlayTimeManager 3.6.5 is deferred: its installed artifact has no dedicated stable public service/API needed by MaddPrestige, while the accepted Paper provider already reads authoritative `PLAY_ONE_MINUTE`. | Avoids a redundant PAPI-derived metric and does not advertise fake plugin support. |
| D-074 | Phase 5 implementation | Phase 5 third-party artifacts are compile/test `provided` dependencies and are excluded transitively. The V2 integration factories remain disconnected from the frozen V1 production bootstrap until the authorized composition phase. | MaddPrestige still packages/loads its accepted V1 runtime without optional plugins, no third-party implementation is shaded, and Phase 6 command/UI/bootstrap work is not pulled forward. |
| D-075 | Phase 5 correction pass 1 | Every present Phase 5 configuration object must be a YAML mapping, every scalar retains its declared YAML type, Placeholder input keys must be actual strings accepted by canonical `MetricId`, and failures report the exact configuration path plus expected/actual type. Absent optional mappings alone receive dormant defaults; explicit null is rejected. | Malformed HOT_RELOAD input cannot silently collapse into a disabled integration, and schema/compiler/runtime semantics remain aligned without a parallel identifier grammar. |
| D-076 | Phase 5 correction pass 1 | `IntegrationProviderLifecycle` exposes exact-set reconciliation, not additive activation. Each apply activates newly reachable bindings, leaves unchanged active bindings and generations untouched, and deactivates bindings removed from the current plan. Only unregister/rebind creates a new generation. | Reapplying a configuration is deterministic/idempotent, and removing a HOT_RELOAD reference cannot leave stale provider reachability active. |
| D-077 | Phase 5 correction pass 1 | Placeholder input recursion protection parses each closed `%identifier_parameters%` token and compares the expansion identifier to `maddprestige` case-insensitively. It does not reject another expansion merely because `maddprestige` occurs in that expansion's parameters. | PlaceholderAPI's case normalization cannot bypass the self-recursion boundary, while unrelated placeholders remain valid. |
| D-078 | Phase 5 correction pass 1 | D-071's EconomyShopGUI progression metric is withdrawn. The verified public `PostTransactionEvent` remains only as optional compatibility diagnostics with no manual-progress/provider/reward/cost handle and hard zero credit; progression configuration is fixed false and true is rejected. | `getPrice()` and mixed `getPrices()` economy units cannot be conflated as money/XP/levels/items/points/custom currency. A later currency-aware design is required before progression can be claimed. |
| D-079 | Phase 5 correction pass 1 | Vault aggregate cost preflight represents one canonical sealed operation batch and therefore requires identical player UUID, operation ID, configuration revision, and provider generation across every action before any Vault call. | Malformed mixed batches fail closed with zero balance/precision/mutation calls; valid coherent batches retain one exact aggregate balance check. |
| D-080 | Phase 5 correction pass 2 | Registry/configuration activation and operational dependency health are independent authorities. Exact-set reconciliation changes only `ActivationState`; discovery preserves the health established by dependency acquisition, config off/on preserves outage state, and only an explicit exact-binding recovery probe or a newly acquired rebind may restore usable health. | Configuration cannot fabricate recovery from `UNHEALTHY`, `UNAVAILABLE`, `DEGRADED`, `UNSUPPORTED`, `INACTIVE`, or `NOT_INSTALLED`. Unchanged bindings retain generation; unregister/rebind alone advances it. |
| D-081 | Phase 5 correction pass 2 | Phase 5 provider-local usability uses one fail-closed allowlist: only `AVAILABLE` and `ACTIVE` are usable. A `ProviderRegistrationGate` binds one registry token/generation plus its operational health and requires current active registry acceptance before a scheduled external call or persistent mcMMO event mutation. | Vault, mcMMO and Placeholder input cannot treat an unenumerated bad state as healthy. Deferred Placeholder tasks and stale mcMMO listeners stop at outage, config deactivation, unregister or rebind; only the current healthy generation resumes. |
| D-082 | Phase 5 correction pass 2 | mcMMO `skill_level` and `power_level` are externally owned current absolute observations, so their descriptors are `NON_MONOTONIC` with reset policy `NOT_APPLICABLE`. The separate adjusted-XP manual total remains MaddPrestige-owned monotonic accumulated progress. | Legitimate mcMMO stat loss is accepted as a lower current value instead of falsely demanding reset reconciliation, without weakening the authenticated monotonic XP event counter. |
| D-083 | Phase 6 implementation | Commands, GUI actions, setup, introspection, diagnostics and direct administration delegate to one platform-neutral administration service layer. Paper adapters translate audience, scheduling and inventory events only; they do not own configuration or progression rules. | A YAML edit, command edit and GUI edit reach the same canonical candidate/compiler/apply path, so interfaces cannot acquire independent semantics. |
| D-084 | Phase 6 implementation | Configuration editing is a named in-memory draft over an immutable active snapshot. Apply always validates the complete multi-document candidate, prepares an immutable on-disk revision, records durable history, atomically switches the `active-revision` pointer and only then publishes runtime state. | Drafts cannot affect production; partial multi-file activation is impossible; a failure retains exact attempted/failed evidence and the last complete revision remains recoverable. |
| D-085 | Phase 6 implementation | Configuration history stores exact UTF-8 documents, canonical hash, actor, source revision, outcome, timestamp and detail. Rollback copies an earlier valid document set into a newly validated revision rather than moving history backward. | Every active state has append-only provenance, rollback remains auditable, and older immutable evidence is never rewritten. |
| D-086 | Phase 6 implementation | Comment/order/unknown-key preservation uses the accepted surgical scalar editor. Structural changes that cannot be represented losslessly are performed only by a purpose-built canonical workflow such as setup or stage-remap; arbitrary structural text rewriting is not advertised as safe. | A38 is protected without pretending that all YAML restructuring is lossless. Unsupported shapes fail actionably instead of normalizing operator-authored files. |
| D-087 | Phase 6 implementation | Consequential rank-up and Prestige confirmations are opaque server-side records bound to actor UUID, operation kind, exact sealed authorization, source/config revisions and expiry. They are single-use and revalidate the active revision before dispatch. | Chat text, GUI items and client-supplied fields never become execution authority; replay, transfer, expiry and stale configuration fail before mutation. |
| D-088 | Phase 6 implementation | GUI inventories contain only display material. Every click resolves an opaque server-side session/action ID, rechecks permission and revision, and rejects shift-click, number-key, double-click, offhand, drag and player-inventory transfer paths. | Inventory metadata is presentation rather than authority and known item-transfer escalation paths cannot trigger privileged work. |
| D-089 | Phase 6 implementation | Setup discovers registered provider capabilities, accepts existing external group names and validates them through the rank adapter during preview/apply. It never creates LuckPerms groups. Its generated baseline keeps requirements, costs, rewards, Prestige, seasons and optional integrations disabled unless explicitly configured. | A fresh simple ladder is safe and provider-aware while retaining the non-creation mandate and avoiding surprise progression features. |
| D-090 | Phase 6 implementation | `/doctor` composes bounded independent probes for configuration history, database health, provider health and exact rank targets. `/why` renders the canonical immutable authorization/explanation result and never reimplements requirement logic. | Diagnostics identify the affected path/stage/provider with remediation while remaining read-only and consistent with consequential authorization. |
| D-091 | Phase 6 implementation | Manual Prestige administration performs one optimistic compare-and-set and appends the complete actor/target/old/new/reason/timestamp audit row in the same SQLite transaction. | Concurrent staff edits cannot overwrite each other and a successful direct change cannot exist without durable audit evidence. |
| D-092 | Phase 6 scope boundary | The new Paper command/GUI adapters are compiled and contract-tested but are not registered by the frozen V1 bootstrap or descriptor. Production V2 composition, live Paper/LuckPerms qualification and the final default switch remain explicitly outstanding; Phase 6 does not modify protected V1 to manufacture deployment evidence. | Phase 6 acceptance is claimed only at the implemented module boundary. A01/A02 and other live-runtime qualifications remain `Partial` until an authorized composition phase wires and exercises them. |
| D-103 | Phase 6 correction pass 2 | SQLite migration 8 provides one canonical stage-transition fence shared by destructive configuration remaps and every authoritative current-stage mutation. Stage operations acquire a durable target-stage lease before their operation journal, cost, projection, internal commit or reward; remap migration and lease acquisition serialize under `BEGIN IMMEDIATE`. | If an operation wins, removal fails before rows move. If remap wins, its durable `MIGRATED_PENDING_CONFIG` journal blocks old-stage entry before any consequential effect, including after restart. No late-write-only check is treated as sufficient. |
| D-104 | Phase 6 correction pass 2 | The remap journal states are `MIGRATED_PENDING_CONFIG`, `CONFIG_APPLIED` and `CONFIG_FAILED_SAFE`. Pending is a durable fence. Applied and failed-safe are terminal only after the new configuration is authoritative or the old pointer/runtime is safely authoritative with projection-equivalent replacement references. A failed pointer restore leaves the journal pending for explicit reconciliation. | SQLite, filesystem pointer and runtime publication are coordinated but are not one physical transaction. Crash/restart preserves the pending fence; uncertain operation leases are retained and their operation journals remain the recovery authority. |
| D-105 | Phase 6 correction pass 2 | Draft provenance immutably assigns `NORMAL`, `ROLLBACK` or `SETUP` at server-side creation. Their exact permissions are respectively `CONFIG_APPLY`, `CONFIG_ROLLBACK` and `SETUP`; preview/acknowledgement/apply/confirmation derive the kind, reject cross-kind methods, and recheck current permission at final mutation. | A caller cannot relabel a normal edit as rollback, a rollback as normal, or setup as either. Revocation after acknowledgement fails closed, while actor binding and compare-and-remove preserve replay/concurrency safety. |
| D-106 | Phase 6 correction pass 2 | GUI acknowledgement/apply derives the sealed draft kind through `GuiConfigurationAuthority`; no GUI item or client argument chooses it. The contextless destructive stage-editor overload is removed. Existing candidates, including rollback drafts whose historical documents already omit a referenced stage, accept remap selection only through a draft-bound server operation that invalidates and reseals preview. | Command, GUI and direct administration share the same normal/rollback/setup permission class and the same canonical remap plan. Arbitrary client-submitted preview plans and contextless destructive actions are not public authority surfaces. |
| D-107 | Phase 6 correction pass 2 | Completion refresh remains an explicit adapter/composition contract over immutable cached authority. Phase 6 implements and tests the refresh/suggest boundary but does not claim automatic live provider, draft or revision synchronization from the intentionally disconnected production bootstrap. | Keystroke completion remains bounded and side-effect free without overstating live V2 wiring that belongs to a later authorized composition phase. |
| D-108 | Phase 6 correction pass 3 | A durable stage-transition permit represents exact `(operation ID, source/current stage, target/result stage, configuration revision, lease token)` participation. Destructive remap start and lease acquisition serialize under the same SQLite `BEGIN IMMEDIATE` protocol and compare every removed source against both lease stages. | An already-authorized rank-up, Prestige reset or projection cannot continue from a stage being removed merely because its target remains valid. The ordering loser fails before costs or external projection. |
| D-109 | Phase 6 correction pass 3 | Durable stage leases are operation-journal-owned. Executors persist `PREPARED` first, then acquire participation before effects. Acquisition requires that exact nonterminal owner; restart adopts the same token; nonterminal release is intentionally a no-op; terminal completion, compensation, failure or evidence-backed reconciliation permits exact idempotent deletion. | A crash-before-journal lease is impossible by construction, a restarted owner can release its adopted lease, and unresolved external uncertainty remains fenced without an age-based correctness heuristic. |
| D-110 | Phase 6 correction pass 3 | Migration 9 replaces target-only leases with source-and-target operation-owned rows. It retains only legacy leases with nonterminal journal owners, marks their source participation incomplete, discards absent/terminal owners deterministically, and requires exact adoption to fill the source before normal use. | Upgrading from migration 8 neither silently deletes potentially live authority nor preserves a permanent ownerless blocker. Doctor can distinguish active, incomplete, absent-owner, terminal-owner and unresolved reconciliation states. |
| D-111 | Phase 6 correction pass 3 | Direct player-stage writes and the nested Prestige lifecycle commit do not create random durable leases. They share the remap store's database-level serialization by opening `BEGIN IMMEDIATE`, determining/checking authoritative source and target participation inside that transaction, and committing or rolling back as one unit. | Import/bootstrap/direct/manual repository APIs cannot race a destructive remap and cannot strand an unjournaled durable row on process crash. The outer journaled executor remains the only durable owner for consequential operations. |
| D-112 | Phase 6 correction pass 3 | Draft remap state is an accumulated server-owned map. Selecting a source merges or replaces only that source, removing a source preserves the rest, and the complete result is validated against absent sources plus enabled ordered targets before draft CAS. Every change increments the draft version and invalidates preview/acknowledgement authority. | Rollback or another candidate may safely migrate multiple removed referenced stages in one sealed preview/apply, while an invalid later mapping cannot destroy an earlier valid selection. Command and GUI use the same source-scoped operations. |
| D-113 | Phase 6 correction pass 4 | A destructive remap target must exist, be enabled and be ordered under both the currently authoritative configuration and the candidate, and its exact internal/external projection must equal the removed source under both authorities. | A candidate-only, prior-disabled, prior-unordered or projection-changed target cannot become durable player state before activation. If activation fails, migrated references remain valid under fallback authority without inventing inverse migration. |
| D-114 | Phase 6 correction pass 4 | Every removed or disabled stage, including a stage with zero references, is reserved as one configuration-owned scope. Its durable owner is the exact `ATTEMPTED` configuration-history revision. Acquisition, terminal operation-lease cleanup, owner validation, scope insertion, unsafe-reference revalidation and any remap execute in one SQLite `BEGIN IMMEDIATE` transaction. | No first reference can arrive between final validation and reservation, and mixed referenced/unreferenced candidates cannot expose an unreserved stage. The history row supplies restart identity without a second competing configuration journal. |
| D-115 | Phase 6 correction pass 4 | Configuration reservations and operation leases remain distinct durable concepts but serialize through the same SQLite boundary. A configuration acquisition rejects a source or target operation lease intersecting its full unsafe set; operation acquisition and every direct stage writer reject the exact active reserved stage before effects/write. Unrelated operation stages remain admissible. | Ownership and recovery semantics stay truthful: journaled operations own source/target participation, while a configuration revision owns removal/disablement authority. This closes rank-up, Prestige, projection, import, direct CAS and remap races without a global healthy-transition lock. |
| D-116 | Phase 6 correction pass 4 | Configuration authority releases only after durable `APPLIED` history plus candidate pointer/runtime coherence, or durable `FAILED` history plus prior pointer/runtime coherence. Mismatch becomes `NEEDS_RECONCILIATION`; recovery compares history, atomic pointer and canonical runtime and uses no elapsed-time heuristic. | A crash before activation resolves to the prior authority, a completed activation resolves to the candidate, and pointer/runtime ambiguity retains the reservation. Repeated terminal cleanup is idempotent and cannot be used to adopt or release a terminal owner prematurely. |
| D-117 | Phase 6 correction pass 4 | Forward-only SQLite migration 10 adds configuration-transition owner, immutable declared scope and active per-stage reservation tables with ownership constraints and indexes. A legacy pending/failed-safe remap can prove only its known remap sources, so it migrates as `UNKNOWN_LEGACY`, incomplete and `NEEDS_RECONCILIATION`. | Migration does not guess the zero-reference portion of a legacy transition. Unknown scope blocks stage writes globally until authoritative recovery; existing migration checksums and supported upgrade paths remain unchanged. |
| D-118 | Phase 6 correction pass 4 | Declared configuration scope, active reservation rows and `ATTEMPTED` history ownership are a bidirectional integrity invariant. Missing/extra reservations, incomplete scope, missing/terminal active owner, ownerless legacy remap and terminal status with lingering authority all fail globally closed and are surfaced as abnormal Doctor state. | Database damage, interrupted/manual cleanup or terminal-owner residue cannot silently reopen one unsafe stage. Healthy complete short-lived authority remains a warning, while contradictory authority is blocked and recoverable only from durable evidence. |
| D-119 | Phase 7 composition | The active Paper descriptor uses `MaddPrestigeV2Plugin`. Startup orders missing-default preparation, SQLite/migrations, canonical compilation, registry, built-in providers, optional discovery, recovery and a minimal diagnostics/reload command before ready; shutdown reverses owned resources. | A01 and A73 can be qualified against the real V2 entry point without claiming the complete Phase 6 setup/GUI surface is live-bound. |
| D-120 | Phase 7 optional lifecycle | GP 16.18.7, deployed WG `7.0.18+2392-fa605e6` and CE 26.7.4 are exact runtime identities. Linked classes are isolated behind post-discovery bootstraps, dependencies are provided, and enable/disable/config reload/CE reload change provider generations. | Absence and incompatible linkage are capability-local; stale sealed authority fails; no optional API is shaded or eagerly resolved. |
| D-121 | Phase 7 GriefPrevention | Remaining/accrued/bonus claim blocks and owned claim count are native read-only metrics. Bonus claim blocks are a native public setter/save reward that is non-idempotent, non-reversible and non-reconcilable; post-mutation failure is uncertain. | Stable useful public surfaces are used without inventing atomicity, provider receipts, replay safety or claim ownership. |
| D-122 | Phase 7 WorldGuard/Paper worlds | Exact configured-region membership is a native read-only WG predicate. Missing authoritative region state is unavailable, a different world is false, and simple world name/UUID/environment/set membership is a generic Paper concern. | MaddPrestige never creates/deletes regions, edits flags or owns world lifecycle/protection. |
| D-123 | Phase 7 CraftEngine | Exact public CraftEngine identity and build APIs support a storage-only current count and an inventory item reward. The reward is capacity-preflighted, exact-identity verified, non-idempotent/non-reversible/non-reconcilable, never drops leftovers and preserves uncertainty. | Ordinary visual lookalikes fail exact identity while privileged raw-state manipulation remains inside the trusted server boundary. |
| D-124 | Phase 7 CraftEngine independent dispositions | Read-only item requirement is native; ordinary item reward is native; an item-backed collectible reuses it; consumable cost/turn-in and permanent non-item cosmetic entitlement remain unavailable. | The qualified API lacks atomic debit/receipt/rollback/reconciliation and permanent account-entitlement contracts, so read capability does not authorize unsafe mutation. |
| D-125 | Phase 7 generic fallbacks | AdvancedCrates, UMC and Discord use only accepted typed Placeholder or reviewed command fallbacks. AxPlayerWarps remains unavailable; AxSellWands and MaddKraft custom plugins are coexistence-only. | Undocumented/internal APIs and pre-credit events never become progression authority. |
| D-126 | Phase 7 ownership and API fit | A71 is an exact data fixture, A72 preserves `mad_hatter` and unrelated LP state, A74 keeps resource worlds external, and A75 proves a fake Court metric through the unchanged registry. `GENERIC INTERFACE EXTENSION REQUIRED: NO`. | Vendor concepts stay out of API/core/persistence and no Phase 8 freeze or Phase 9 gameplay tuning is pulled forward. |
| D-127 | Phase 7 A73 authority | The owner-supplied 47-JAR snapshot and exact hashes are authoritative. Qualification copies it to a disposable directory, replaces only the old MaddPrestige JAR, tests Paper-only absence and explicitly enabled native providers, and never redistributes third-party JARs. | The evidence distinguishes current deployed artifacts from historical/candidate inputs and leaves the snapshot read-only. |
| D-128 | Phase 7 correction pass 1 composition | The active V2 composition constructs every accepted Phase 5 capability: Vault metric/cost/reward, mcMMO metrics/authenticated XP events, typed scheduled PAPI inputs, cache-only output expansion and zero-credit EconomyShopGUI/QuickShop listeners. | Valid accepted settings cannot be operationally inert; absence/version/linkage/service failure remains capability-local and generation/health-gated. |
| D-129 | Phase 7 correction pass 1 configuration authority | The Phase 7 live reload command is non-mutating and refuses direct file read/compile/reconcile/swap. Only the accepted Phase 6 draft/preview/acknowledgement/apply/history/remap route may publish configuration. | A second shortcut cannot bypass destructive-transition reservations, active pointer/history coherence or A69 concurrency. A02 remains Partial rather than inventing broad live administration. |
| D-130 | Phase 7 correction pass 1 CraftEngine readiness | CraftEngine state is `ABSENT`, `WAITING_FOR_REGISTRY`, `AVAILABLE` or `DISABLED_OR_UNAVAILABLE`. Enable alone never proves readiness; startup may use only a non-empty public registry probe, while re-enable requires a public completed reload. Every completed reload revalidates configured IDs and replaces provider generations. | Empty/transient startup state cannot become false missing-item evidence, and no old definition/build authority can mutate after reload. |
| D-131 | Phase 7 correction pass 1 hard gates | A71 requires full exact-ladder progression, Prestige baseline reset and SQLite reopen; A72 requires supporter/unrelated/contextual isolation across progression, Prestige and reconciliation; A75 requires the complete generic provider configuration/health/generation lifecycle. | Compilation-only or happy-path evidence is insufficient, and no MaddKraft/Court concept is added to generic production code. |
| D-132 | Phase 7 correction pass 1 A73 | A73 evidence classifies all 47 artifacts, runs Paper-only/full/GP-absent/CE-absent/WG-absent/WE-absent cases, proves live Phase 5/7 binding and exercises a real CE item through production read/reward/capacity/identity/reload/lifecycle boundaries. | Provider-active logs alone are insufficient. A disposable first-party harness may invoke actual production boundaries, but third-party JARs and secrets are never bundled. |

## Deferred choices

| ID | Deferred choice | Phase/evidence needed | Binding constraint |
|---|---|---|---|
| D-093 | Phase 6 correction pass 1 (refined by D-103/D-104) | Referenced-stage deletion is authorized only by a `StageRemapSnapshot` sealing the exact plan, per-player stage/state/config revisions, counts and content hash. SQLite migration 7 journals the operation and entries and migrates all rows with CAS in one `BEGIN IMMEDIATE` transaction before configuration pointer activation. | A valid replacement cannot orphan current state. Stale rows roll back the whole SQL transaction. D-104 now distinguishes known-safe `CONFIG_FAILED_SAFE` from incomplete `MIGRATED_PENDING_CONFIG`, which remains durably fenced. |
| D-094 | Phase 6 correction pass 1 | Every draft has a monotonically increasing server version. Preview binds draft ID/version/hash/base/actor/remap seal; finalization atomically claims that exact draft before any durable write, and successful activation consumes it. | Edits, cancellation and two concurrent applies cannot revive, overwrite or double-apply an older same-draft candidate. |
| D-095 | Phase 6 correction pass 1 | High-risk configuration/setup acknowledgement is a separate server-issued authority bound to actor, draft version/hash, base revision, apply kind, exact finding seal/codes and five-minute expiry. Caller-supplied acknowledgement codes are rejected. | Command and GUI can confirm legitimate risk without allowing arbitrary text to manufacture authorization; wrong-actor, stale, expired, concurrent and replay use fail closed. |
| D-096 | Phase 6 correction pass 1 | GUI mutation actions retain opaque server-side context and `CanonicalGuiMutationExecutor` delegates scalar/list/stage/preview/acknowledgement/apply/rollback work to `ConfigurationAdministrationService`. `/gui` responses carry the real view and the Paper adapter opens it through `PaperPhaseSixGuiController` when bound. | Direct, command and GUI surfaces produce one canonical candidate and retain actor/permission/revision checks without trusting inventory metadata. Live V1 bootstrap registration remains out of scope. |
| D-097 | Phase 6 correction pass 1 | Structural administration owns lossless block-list/map/stage operations only. Completion uses cached schema/provider/stage/draft/revision authority and advertises only executable positions; generic map mutations are not suggested. | YAML comments, order, CRLF and empty collection types survive supported edits. Keystroke completion makes no database or provider call and cannot advertise a route the dispatcher rejects. |
| D-098 | Phase 6 correction pass 1 | Setup sessions are owner-bound, resumable server state with two-hour expiry and explicit cancel. The simple wizard owns provider/stages/baseline plus one requirement, cost, reward and enabled/disabled Prestige eligibility/reset configuration, all previewed/applied canonically. | Setup becomes useful without becoming a full YAML editor or creating external groups. A02 remains Partial until production composition/live qualification. |
| D-099 | Phase 6 correction pass 1 | Doctor declares 18 diagnostic domains. Missing probes produce explicit deferred `doctor.not_checked.*` findings, so broad `HEALTHY` is possible only when every applicable domain was checked. | Schema, metrics, costs/rewards, operations/reconciliation, orphan/duplicate/integrity, Placeholder, scheduler/cache, flush and unsupported capability blind spots are visible rather than silently healthy. |
| D-100 | Phase 6 correction pass 1 | A prepared filesystem revision is reverified immediately before pointer switch against the exact manifest text, flat file inventory, every document byte and canonical hash. Immutable revision/history evidence is not pruned by Phase 6. | Post-prepare tampering cannot activate. Retention policy remains deferred rather than introducing unsafe cleanup in the activation path. |
| D-101 | Phase 6 correction pass 1 | Operation and configuration authorities check actor before removal, use conditional expiry/stale removal and compare-and-remove consumption. GUI/draft/setup sessions are pruned by bounded expiry. | A wrong actor cannot consume the rightful actor's token, and exactly one concurrent confirmation wins. Expired authority cannot remain usable indefinitely. |
| D-102 | Phase 6 correction pass 1 | The older `StageConfigurationWorkflow` remains read-only and emits `stage.change.remap_execution_required`; only `PhaseSixConfigurationWorkflow` backed by `StageReferenceMigrationStore` may execute referenced remaps. | Earlier call sites cannot accidentally interpret a remap plan as evidence that persistence was migrated. The correction adds capability without weakening the legacy fail-closed boundary. |
| O-004 | Comment-preserving YAML implementation library/strategy | Resolved by D-031 | selected only after the Phase 1 golden spike passed |
| O-005 | Curious/Odd/Mad/Unbound migration mapping | Phase 2/9 plus fixtures and explicit owner decision | never infer or auto-create old groups |
| O-007 | Supporter display names and entitlement mappings | later MaddKraft preset phase | must not block generic core or restore old patron ownership |
| O-008 | MaddKraft costs, requirements, scaling, currency cadence, and milestones | Phase 9 tuning | derive from real data; do not freeze prototype values |
| O-009 | Exact MySQL/MariaDB versions and CI services | before either backend is publicly claimed | contract-test every publicly claimed backend/version family |
| O-011 | User-facing retention durations/tuning | later administration/public-release work | Phase 1 must establish protection/redaction/schema invariants first |
