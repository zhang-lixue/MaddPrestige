package net.maddkraft.maddprestige.core.admin.config;

/** Why a currently referenceable stage is unsafe in a pending configuration. */
public enum ConfigurationStageReservationKind {
    REMOVED,
    DISABLED,
    UNKNOWN_LEGACY
}
